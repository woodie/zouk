public enum AppInfo {
    /// Matches macOS's own "Hide Zouk"/"Quit Zouk".
    public static let shortName = "Zouk"

    /// Shown in the window title and About panel, never in a menu item's own label.
    public static let fullName = "\(shortName) scan retriever"
}
